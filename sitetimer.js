const intege = Math.floor(Math.random() * 4500) + 1;


    setTimeout(function () {
        {
            document.getElementById("enter").play();  
            document.getElementById("btn").style.opacity = 1;
            document.getElementById("logo").style.opacity = 1;
        }
    }, intege);
